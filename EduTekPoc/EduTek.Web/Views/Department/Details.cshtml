@model EduTek.Web.Models.DepartmentViewModel

@{
    ViewData["Title"] = "Department Details";
    var role = Context.Session.GetString("Role");
}

<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card shadow-sm border-0">
            <div class="card-body p-4">
                <h1 class="h3 mb-3">Department Details</h1>

                <dl class="row mb-4">
                    <dt class="col-sm-4 text-muted">Department ID</dt>
                    <dd class="col-sm-8 fw-semibold">@Model.DepartmentId</dd>

                    <dt class="col-sm-4 text-muted">Department Name</dt>
                    <dd class="col-sm-8">@Model.DepartmentName</dd>

                    <dt class="col-sm-4 text-muted">Description</dt>
                    <dd class="col-sm-8">@Model.Description</dd>
                </dl>

                <div class="d-flex gap-2">
                    <a asp-controller="Department" asp-action="Index" class="btn btn-secondary">
                        Back to List
                    </a>
                    @if (role == "Admin")
                    {
                        <a asp-controller="Department" asp-action="Edit" asp-route-id="@Model.DepartmentId" class="btn btn-warning">
                            Edit
                        </a>
                    }
                </div>
            </div>
        </div>
    </div>
</div>